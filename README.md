# QR-Code-Generator
Gemini said This Python script uses the qrcode library to generate PNG images from URLs. It features both a direct implementation for a LinkedIn profile and a flexible function, qr_code_gen(), which allows users to input any link and save it under a custom filename.
